import 'package:flutter/widgets.dart';

class Sceincescreen extends StatelessWidget {
  const Sceincescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Sceince Screen"));
  }
}